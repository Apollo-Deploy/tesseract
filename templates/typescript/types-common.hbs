/**
 * @generated
 * This file was automatically generated by sdk-forge.
 * DO NOT MODIFY THIS FILE DIRECTLY.
 */

// ── Pagination ──────────────────────────────────────

/**
 * Pagination query parameters shared across all list endpoints.
 */
export interface PaginationQuery {
  /** Page number (1-indexed) */
  page?: number;
  /** Number of items per page */
  limit?: number;
  /** Column name to sort by */
  sortBy?: string;
  /** Sort direction */
  sortOrder?: 'asc' | 'desc';
}

/**
 * Page metadata returned by paginated endpoints.
 */
export interface PageMeta {
  /** Number of items in this page */
  size: number;
  /** Total number of pages available */
  totalPages: number;
  /** Whether more pages exist after the current one */
  hasMore: boolean;
}

/**
 * Generic paginated response wrapper.
 *
 * All list endpoints return data in this shape: an array of items
 * plus a `page` object with pagination metadata.
 *
 * @template T - The type of items in the data array
 */
export interface Page<T> {
  data: T[];
  page: PageMeta;
}

// ── Common Responses ────────────────────────────────

/**
 * Generic success response returned by mutation endpoints.
 */
export interface SuccessResponse {
  success: boolean;
  message?: string;
}

/**
 * Message-only response returned by various endpoints.
 */
export interface MessageResponse {
  message: string;
}

// ── Error Envelope ──────────────────────────────────

/**
 * Raw error response envelope returned by the API.
 *
 * This interface represents the raw JSON shape of error responses.
 * It is used internally by the SDK to construct `SDKError` instances.
 */
export interface ApiErrorEnvelope {
  /** Machine-readable error code */
  error: string;
  /** Human-readable error message */
  message: string;
  /** HTTP status code */
  status: number;
  /** Unique request identifier for debugging */
  request_id?: string;
  /** ISO 8601 timestamp of error occurrence */
  timestamp?: string;
  /** Request path */
  path?: string;
  /** HTTP method */
  method?: string;
  /** Helpful hint for resolving the error */
  hint?: string;
  /** Source file location (development only) */
  file?: string;
  /** Function name (development only) */
  function?: string;
  /** Structured error details (4xx only) */
  details?: unknown;
  /** Stack trace (development only, 5xx errors only) */
  stack?: string;
}
